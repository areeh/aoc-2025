# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# AoC input files, to honor creator's wish to not publish
*/inputs/*.txt

# dotenv for local secrets
.env
